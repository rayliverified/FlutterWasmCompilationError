import 'package:flutter/material.dart';

const double cardRadiusValue = 10;
const Radius cardRadius = Radius.circular(cardRadiusValue);
const BorderRadius cardBorderRadius = BorderRadius.all(cardRadius);
const ShapeBorder cardShapeBorder =
    RoundedRectangleBorder(borderRadius: cardBorderRadius);

const double buttonRadiusValue = 5;
const Radius buttonRadius = Radius.circular(buttonRadiusValue);
const BorderRadius buttonBorderRadius = BorderRadius.all(buttonRadius);
const OutlinedBorder buttonShapeBorder =
    RoundedRectangleBorder(borderRadius: buttonBorderRadius);

const double inputFieldRadiusValue = 5;
const Radius inputFieldRadius = Radius.circular(inputFieldRadiusValue);
const BorderRadius inputFieldBorderRadius = BorderRadius.all(inputFieldRadius);
const ShapeBorder inputFieldShapeBorder =
    RoundedRectangleBorder(borderRadius: inputFieldBorderRadius);

const double groupRadiusValue = 8;
const Radius groupRadius = Radius.circular(groupRadiusValue);
const BorderRadius groupBorderRadius = BorderRadius.all(groupRadius);
const RoundedRectangleBorder groupShapeBorder =
    RoundedRectangleBorder(borderRadius: groupBorderRadius);

const double circularRadiusValue = 90;
const Radius circularRadius = Radius.circular(circularRadiusValue);
const BorderRadius circularBorderRadius = BorderRadius.all(circularRadius);
const RoundedRectangleBorder circularBorderShape =
    RoundedRectangleBorder(borderRadius: circularBorderRadius);
